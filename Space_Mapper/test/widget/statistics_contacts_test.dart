// COMMENTING OUT BECAUSE NEED TO CHANGE CHART PACKAGE


/*
import 'package:asm/ui/statistics/statistics.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('StatsContacts displays all the stats',
      (WidgetTester tester) async {
    // Create the widget by telling the tester to build it.
    await tester.pumpWidget(MyStatistics());

    // Create the Finders.
    final avgContacts = find.text('Avg Contacts');
    final totalContacts = find.text('Total Contacts');

    // that the Text widgets appear exactly once in the widget tree.
    expect(avgContacts, findsOneWidget);
    expect(totalContacts, findsOneWidget);
  });
}
*/
